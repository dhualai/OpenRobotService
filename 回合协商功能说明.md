# 工单「回合协商」功能说明

> 版本：v1.0 ｜ 适用范围：`OpenRobotService` 全栈（后端 / AI 服务 / 前端）
> 关联文件：`func_.md`、`tasks_step.json`、`backend/app/modules/tasks/api/task.py`、`frontend/src/pages/tasks/TaskDetailPage.tsx`、`frontend/src/shared/utils/ticketFilters.ts`

---

## 一、功能概述

「回合协商」是在「工单阶段性处理」之上引入的**双向回合制**机制：工单在**接单人**与**提单人**之间以"提案 → 回应"为单位轮流推进，每一轮对应一次节点/时间调整。

核心目标：
1. 让工单的"待处理归属"从静态的"派给谁就归谁"升级为**动态回合制**——接单人更新 step 后，工单自动出现在提单人的「待我处理」中，等待提单人确认/答复。
2. 控制协商成本，避免双方无限打口头仗：达到最大回合后用「升级上报」替代管理员介入，由双方任一主动发起升级。
3. 不阻塞业务：达到阈值**不抛 HTTP 错误**，由前端按钮态引导，保留升级上报"用户主动选择接管人"的灵活语义。

---

## 二、数据模型

### 2.1 Task 表新增三列

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `step_last_updated_by` | `VARCHAR(100)` | NULL | 最近一次改 step 的操作人**侧标识**：`assigned`（接单人侧）/ `creator`（提单人侧），非具体用户ID |
| `step_last_updated_at` | `DATETIME` | NULL | 最近一次 step 更新时间 |
| `step_negotiation_round` | `INT` | 0 | 协商回合数；对手回应一次 +1 |

> 「侧标识」与 `assigned_to` / `created_by` 字段无关，仅表示角色侧，便于判定工单当前该轮到哪一方。

### 2.2 全局配置

`backend/app/core/config.py`：
```python
TICKET_STEP_MAX_NEGOTIATION_ROUNDS: int = Field(default=5)
```
含义：接单人↔提单人来回应答最大次数（含首次）。建议默认 5 轮——已覆盖"一次分歧 + 两次迭代"的常规业务协商，超出通常意味着分歧较大应转升级上报。

### 2.3 响应字段

`TicketResponse` / `TicketListItemResponse` 暴露：
- `step_last_updated_by`：侧标识
- `step_last_updated_at`：更新时间（naive UTC）
- `step_negotiation_round`：当前回合数
- `step_neg_max_rounds`：该工单适用上限（默认读全局配置，Task 模型用 `@hybrid_property` 计算）

### 2.4 迁移

`backend/alembic/versions/e1f2a3b4c5d6_add_task_step_negotiation_columns.py`
幂等 add_column 三列。部署需执行：
```bash
cd backend && alembic upgrade head
```

---

## 三、回合计数规则

### 3.1 写位点（三接口共用）

辅助函数 `_apply_step_update_meta(ticket, current_user, username)`（`backend/app/modules/tasks/api/task.py`）：

1. **判定当前操作人侧**：`_actor_side()` 返回 `assigned` / `creator` / None。
2. **对手回应 → 回合 +1**：本次操作方与上次（`step_last_updated_by`）不同侧时，`step_negotiation_round += 1`。
3. **同方连续操作不计数**：接单人连续两次改协商时间，回合不增（避免刷数）。
4. 写入 `step_last_updated_by` / `step_last_updated_at`。

### 3.2 各接口行为

| 接口 | 行为 |
|---|---|
| `POST /{task_id}/respond` 首次响应 | 写 `step_last_updated_by = assigned`，**不计回合**（业务启动不算交涉） |
| `POST /{task_id}/negotiate-step` 协商节点 | 按规则计回合；同时开放提单人协商权限（assigned/creator/admin/操作权限均可） |
| `POST /{task_id}/complete-step` 阶段完成 | 同上计回合 |

### 3.3 回合流转示例

```
round=0  updated_by=NULL        → 接单人 A 协商(首次提案)
  → round=0, updated_by=ASSIGNED → 提单人 B「待我处理」
round=0  updated_by=ASSIGNED   → B 回复协商
  → round=1, updated_by=CREATOR → A「待我处理」
round=1  updated_by=CREATOR    → A 再次协商
  → round=2, updated_by=ASSIGNED → B「待我处理」
... 以此类推，直至 round=maxRound
```

---

## 四、阈值策略（递进式，不阻塞）

按经验采用"软提醒 → 最后一轮硬引导"两档：

| 状态 | round 阈值（默认 max=5） | 行为 |
|---|---|---|
| 正常交涉 | `round < max-1` 即 ≤3 | 无提示，正常落库 |
| 软提醒 | `round == max-1` 即 =4 | 操作成功，系统评论追加「⚠️ 已临近最大协商回合（4/5），请尽快收敛；若仍无法达成一致可使用升级上报」 |
| 最后一轮 | `round >= max` 即 ≥5 | **不抛 400**；前端按钮区全部替换为 danger「升级上报」按钮，引导用户发起升级上报替代管理员介入 |

> 不抛 400 的原因：保留升级上报"用户主动选择升级对象"的灵活语义；同时避免"列表能看到但按钮不能点"的困惑。

---

## 五、「待我处理」过滤扩展

### 5.1 后端过滤能力

`backend/app/modules/tasks/services/ticket_service.py` 的 `FIELD_MAPPING` 新增虚拟字段：
```python
'stepUpdatedBy': (Ticket.step_last_updated_by, 'enum'),
```
支持 `eq` / `is_null` / `not_null` 等操作，前端按 `{field:'stepUpdatedBy', op:'eq', value:'assigned'}` 即可匹配"接单人刚更新了 step"。

### 5.2 前端过滤分支

`frontend/src/shared/utils/ticketFilters.ts` 的 `buildRelevanceFilters('mine')` 改为 3 个 OR 分支（删除了原有的重复分支）：

| 分支 | 条件（AND） | 含义 |
|---|---|---|
| 1 | `status ∈ {new,in_progress,pending}` AND `assignedTo=我` | 我是处理人，等我干活（原逻辑） |
| 2 | `status=resolved` AND `createdBy=我` | 我是提单人，待我验收（原逻辑） |
| 3 新增 | `status ∈ {new,in_progress,pending}` AND `createdBy=我` AND `stepUpdatedBy='assigned'` | 我是提单人，**接单人刚改过 step，轮到我确认/答复** |

口径与底部导航「待我处理」角标（`MainLayout`）、详情页状态展示完全一致。

---

## 六、详情页 UI

### 6.1 回合胶囊（工单阶段性处理区头部）

位置：`TaskDetailPage.tsx` 工单阶段性处理区顶部右侧。

- 文案：`交涉回合 n / 5`
- 颜色规则：
  - 0-3：灰底（`rgba(100,116,139,0.15)`）
  - 4（临阈值）：黄底（`rgba(234,179,8,0.2)`）
  - ≥5（超限）：红底（`rgba(220,38,38,0.15)`）
  - 轮到当前用户：蓝底（`rgba(37,99,235,0.15)`）
- 侧条提示：
  - 轮到我：`● 轮到你确认/答复`
  - 超限：`● 已达最大回合，请使用升级上报`

### 6.2 阶段性处理按钮区

达到 `round >= maxRound` 时：
- **所有协商按钮（协商节点时间 / 当前阶段完成 / 确认同意 / 工单退回）替换为单个 danger「升级上报」按钮**
- 点击自动带好原因文案 `已达最大协商回合（5/5），申请升级介入。`，走现有升级上报流程

未达上限时按原状态分支：
- 处理中：协商节点时间 ｜ 当前阶段完成
- 新建：工单退回 ｜ 协商节点时间 ｜ 确认同意

### 6.3 底部角色动作栏

达到回合上限时：
- 「升级上报」按钮改 danger 主题、文案变 `升级上报（回合超限）`，原因文案自动填充
- 对**不属于原始 showRoleActions 的提单人**，单独显示一个 danger「升级上报（回合超限）」按钮
- **无需管理员介入，无需 reset-negotiation-round 接口**

---

## 七、接口清单

| 接口 | 方法 | 用途 | 权限 |
|---|---|---|---|
| `/{task_id}/steps` | GET | 读取工单类型的 step 模板 | 登录用户 |
| `/{task_id}/respond` | POST | 首次响应：new→in_progress + 写 step_updated_by=assigned | AI 工单任意用户 / 其余处理人/admin/操作权限 |
| `/{task_id}/negotiate-step` | POST | 协商节点：可调整前/后任一节点 + 设时间 + 理由必填 + 计回合 | AI 工单任意用户 / 其余接单人/提单人/admin/操作权限 |
| `/{task_id}/complete-step` | POST | 当前阶段完成：基于 sequence+1 推进 + 计回合 | 同 respond |
| `/{task_id}/escalate`（现有） | POST | 升级上报：通知指定用户 + 管理员 | 提单人/接单人（达阈值时强制可见） |

> 无 reset-negotiation-round 接口：达阈值由「升级上报」替代管理员介入，符合"无需管理员接入"的产品要求。

---

## 八、操作日志与系统评论

每次协商 / 阶段完成 / 首次响应都会写：

1. **操作日志**（`task_operation_logs`）：
   - `op_type = UPDATE` / `STATUS_CHANGE`
   - `detail` 含 `from_step` / `to_step` / `curr_step_endtime` / `negotiation_round`
   - `description` 中文描述，角色前缀（提单人/接单人/AI 派单）

2. **系统评论**（讨论区）：
   - 协商：`{用户} 协商将节点「A」调整为「B」（节点时间 ...），理由：xxx（本轮协商回合：n/5）`
   - 阶段完成：`{用户} 完成阶段「A」，进入「B」（本轮协商回合：n/5）`
   - 软提醒追加：`⚠️ 已临近最大协商回合，请尽快收敛；若仍无法达成一致可使用升级上报。`

节点时间一律按东八区展示（`_format_shanghai`，DB 存 naive UTC）。

---

## 九、部署与配置

### 9.1 必做

1. 执行迁移：`cd backend && alembic upgrade head`
2. 重启后端服务
3. 前端重新构建部署

### 9.2 可调

- 全局回合上限：修改 `backend/app/core/config.py` 的 `TICKET_STEP_MAX_NEGOTIATION_ROUNDS`（默认 5），重启生效
- 工单专属上限：当前未实现 per-ticket 覆盖，所有工单统一读全局配置；如需差异化可扩展 `step_neg_max_rounds` 列

### 9.3 兼容性

- 既有工单：`step_last_updated_by = NULL`、`step_negotiation_round = 0`，按"默认接单人回合"处理
- 派单后状态保持 new：派单（AI/手动）只写 assigned_to 不改状态，工单保持新建直到首次响应

---

## 十、边界与注意事项

1. **首次响应不算回合**：respond 写 `step_last_updated_by=assigned` 但 round 保持 0；回合从"对手回应"开始计数。
2. **同方连续操作不计数**：避免单方刷数。
3. **AI 工单权限宽松**：`source='ai'` 的工单允许任何登录用户协商/响应（兜底双工单场景）。
4. **不抛 400**：达到最大回合时协商/阶段完成接口仍可调用（后端不阻塞），但前端按钮已替换为升级上报，业务上不会触发。
5. **升级上报替代管理员**：达阈值后双方任一可见 danger 按钮，无需 reset 接口、无需管理员介入。
6. **未跑迁移的兜底**：后端用 `getattr(ticket, 'step_last_updated_by', None)` 读取，但**写入会因缺列报 500**，迁移必须在重启服务后、首次使用前跑完。

---

## 十一、相关文件清单

| 层 | 文件 | 改动 |
|---|---|---|
| 后端模型 | `backend/app/models/task.py` | +3 列 + `step_neg_max_rounds` hybrid_property |
| 后端模型 | `ai/core/database.py` | +3 列（AI 侧同步） |
| 迁移 | `backend/alembic/versions/e1f2a3b4c5d6_*.py` | 新建，幂等 add_column |
| 配置 | `backend/app/core/config.py` | +`TICKET_STEP_MAX_NEGOTIATION_ROUNDS=5` |
| Schema | `backend/app/modules/tasks/schemas/ticket.py` | 暴露回合字段 |
| 后端接口 | `backend/app/modules/tasks/api/task.py` | respond/complete-step/negotiate-step 写回合 + 阈值提示 + `_apply_step_update_meta` |
| 后端服务 | `backend/app/modules/tasks/services/ticket_service.py` | FIELD_MAPPING 加 `stepUpdatedBy` |
| 前端过滤 | `frontend/src/shared/utils/ticketFilters.ts` | mine 分支加提单人回合分支 |
| 前端详情 | `frontend/src/pages/tasks/TaskDetailPage.tsx` | 回合胶囊 + 按钮态 + 升级上报强制可见 |

---

*文档生成时间：2026-09-01*
