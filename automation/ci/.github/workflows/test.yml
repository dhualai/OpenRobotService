name: Test

on:
  push:
    branches: [hxg, develop]
  pull_request:
    branches: [develop]

env:
  PYTHON_VERSION: "3.11"

jobs:
  test-infra:
    name: Infrastructure tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ github.workspace }}

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: openrobot
        ports:
          - "3306:3306"
        options: >-
          --health-cmd "mysqladmin ping -h localhost"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - "6379:6379"
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Start Qdrant
        run: |
          docker run -d --name qdrant -p 6333:6333 qdrant/qdrant:v1.10
          sleep 5

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e backend/
          pip install -e automation/

      - name: Run infrastructure tests
        env:
          PYTHONPATH: ${{ github.workspace }}
          AUTOMATION_ENV: ci
        run: |
          pytest automation/config/tests/ automation/logger/tests/ automation/clients/tests/ automation/assertions/tests/ automation/fixtures/tests/ -v --alluredir=output/allure-results --junitxml=output/junit-infra.xml

  test-api:
    name: API tests (mock)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ github.workspace }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e automation/

      - name: Run API mock tests
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: |
          pytest automation/api/tests/ -m api -v --alluredir=output/allure-results --junitxml=output/junit-api.xml

  test-auth:
    name: Auth tests (real backend)
    runs-on: ubuntu-latest
    needs: test-infra
    defaults:
      run:
        working-directory: ${{ github.workspace }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e backend/
          pip install -e automation/

      - name: Run database migrations
        run: |
          cd backend && alembic upgrade head

      - name: Run auth tests
        env:
          PYTHONPATH: ${{ github.workspace }}
          AUTOMATION_ENV: ci
          MYSQL_HOST: localhost
          MYSQL_PORT: 3306
          MYSQL_USER: root
          MYSQL_PASSWORD: root
          MYSQL_DATABASE: openrobot
        run: |
          pytest automation/api/tests/test_health.py automation/api/tests/test_auth.py -v --alluredir=output/allure-results --junitxml=output/junit-auth.xml

  report:
    name: Allure report
    if: always()
    needs: [test-infra, test-api, test-auth]
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ github.workspace }}

    steps:
      - uses: actions/checkout@v4

      - name: Download Allure results
        uses: actions/download-artifact@v4
        with:
          pattern: allure-results-*
          merge-multiple: true
          path: output/allure-results

      - name: Generate Allure report
        uses: simple-elf/allure-report-action@v1.7
        with:
          allure_results: output/allure-results
          allure_history: allure-history
          keep_reports: 20

      - name: Deploy report to GitHub Pages
        if: github.ref == 'refs/heads/develop'
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: allure-history
