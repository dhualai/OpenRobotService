# OpenRobotService 前端项目文档

## ⚠️ 最高优先级准则（每次对该前端工程编码前必读）

> **所做的所有已提交的代码改动，都要同步更新到本文档下面（「维护历史」一节）。**
> 可以不是代码，但必须体现：哪些功能 / 哪些页面做了什么更新。
> **每次对该前端工程进行编码前，先把本文档读一遍。**
> 若发现本文档与代码不符，以代码为准，并立即修订本文档。

---

## 🔁「帮我提交代码」标准工作流（用户说"帮我提交代码"时照此执行）

1. **查看改动**：`git status --short` + `git branch --show-current`；区分"本次相关改动"与既有无关 WIP。
2. **暂存**：`git add <相关文件>` —— 只加本次相关文件，**不混入无关 WIP**；被 `.gitignore` 忽略的文件自动排除。
3. **提交信息**：`feat：<概括>`，按暂存内容概括，**≤30 字**（含 `feat：`）；类型固定 `feat：`，除非用户另指定。
4. **提交**：`git commit -m "feat：<概括>" -m "Co-Authored-By: Claude <noreply@anthropic.com>"`（第二个 `-m` 为 trailer）。
5. **验证**：`git log -1 --stat` + `git status --short`。
6. **不主动 push**（除非用户明确要求）；若在 `main` 分支则先切功能分支再提交。
7. **同步本文档**：把本次提交补进下方「维护历史」。

---

## 一句话概览

移动端优先的机器人运维工单 + AI 诊断工作台。三大视角收口在一个底部 Tab 工作台里：
**我要摇人**（AI 诊断 + 提单）｜ **系统任务**（AI 任务助手 + 工单列表）｜ **后台管理**（项目/风险/人员/权限等看板）。

技术栈：Vite 7 + React 19 + TS 5.9（strict）+ TDesign Mobile React 0.23 + React Router v7 + Zustand 5 + ECharts 6。
工作目录：`frontend/`。

---

## 常用命令

```bash
npm run dev           # 开发服务器 localhost:5173，/api 代理（见「后端与代理」）
npm run build         # tsc -b 类型检查 + Vite 生产构建（vendor 拆分），默认 base='/'
npm run build:test    # 构建**测试**包：base='/t/app/'（配合 nginx /t/* 分发）
npm run build:prod    # 构建**生产**包：base='/p/app/'（配合 nginx /p/* 分发）
npm run lint          # ESLint flat config，目标退出码 0（0 error）
npm run test          # Vitest 单次（jsdom + Testing Library）
npm run test:coverage # 覆盖率（v8 provider）
```

---

## 路由架构（核心，2026-07-16 重构）

所有业务页统一收口到 `/app`，由 `AuthGuard` 包裹的 `MainLayout`（底部三 Tab）承载：

```
/login, /no-permission                          # 无守卫
/app  → AuthGuard > MainLayout（底部 TabBar）
  ├─ /app/call            → CallView      （我要摇人）
  ├─ /app/call/ticket/:id → TicketDetailPage（摇人「历史工单」详情操作页，全屏）
  ├─ /app/tasks           → TasksView     （系统任务，列表+详情）
  ├─ /app/tasks/:id       → TasksView     （直接打开工单详情）
  └─ /app/admin           → AdminView（入口卡片网格）
       └─ AdminLayout（顶部 Navbar + ☰ 抽屉菜单，无底部 Tab）
            ├─ dashboard / project-manage / project-edit/:id?
            ├─ progress / personnel / project-hr / project-auth
            ├─ risks / risk-edit/:id? / reports
            ├─ data-import / operation-logs
            ├─ users / roles / permissions / resources
            └─ daily-reports                       # 共 17 个 admin 子页
```

**旧路由（`/`、`/home`、`/call`、`/tasks`、`/admin` 及其子路径、`*`）全部 `Navigate` 重定向到 `/app/call`。**
HomePage（旧临时导航页）已移除。路由定义见 `src/router/index.tsx`。

---

## 三大视角与跨视图联动（workbench store）

第二个全局 store `src/stores/workbench.ts` 是三视角数据流转中枢（**全局不止 auth 一个 store**）：

| 字段 | 流向 | 作用 |
|------|------|------|
| `ticketDraft` | Call → Tasks | AI 诊断结果打包成草稿，`goToTab('tasks', {ticketDraft})` 后 TasksView 消费并自动建单 |
| `chatContext` | Tasks → Call | 工单「讨论」按钮带标题/描述回到 AI 对话注入引导语 |
| `selectedTicketId` | — | 建单/跳转后自动展开详情 |
| `tasksRefreshKey` | — | +1 触发 TasksView / HistoryTickets 重新拉取 |
| `activeTab` | — | 与路由 + TabBar 双向同步高亮 |

统一入口 `goToTab(tab, payload)`。消费型字段用 `consumeXxx()`（取出即清空）。

### 三视角页面构成
- **CallView（我要摇人）**：上 60% `ChatPanel`（全屏）+ 下 40% `HistoryTickets`（我提交的工单，虚拟滚动）。
- **TasksView（系统任务）**：上 `ChatPanel compact` + 下工单卡片列表（搜索/状态筛选/分页）+ 右侧 Popup 工单详情（催办/修改/讨论/升级上报）。
- **AdminView**：按角色（`isAdmin`）渲染入口卡片，点击进 AdminLayout 子页。

---

## AI 模块（`src/api/ai.ts`，走 `/api/ai/qa/*` 与 `/api/ai/memory/*`，本地开发后端）

诊断 Agent 接口走 `/qa/*`；历史工单列表走 `/memory/tickets/all`（`/chat/*` 纯对话此前未接线，已移除）：
- `qaAskStream`（SSE 流式，`data.token` 增量）、`qaSubmit`（转工单）、`qaGetTicket`、`qaTicketAck`（派单回执）、`qaUpload`（附件 FormData）。
- `qaListTickets(skip, limit, filters?)`（历史工单列表，GET `/api/ai/memory/tickets/all`；按角色：admin 全部/其余仅本人；`filters` 可选 `status`/`type` 筛选）。

- `session_id` 由前端生成（`generateSessionId`）并存 localStorage（`ai_sessions`，最多 50 个）。
- SSE 用原生 `fetch` + `ReadableStream` 读取（`fetchWithAuth` 带 Bearer token），**不走** `api/client.ts`。
- `ChatPanel`（`src/shared/components/ChatPanel.tsx`）是对接 SSE 的可复用对话面板，call/tasks 两场景共用，功能：流式输出、点赞点踩、复制、修改重发、语音、上传/拍照、ENTER 发送、call 场景转工单。**对话历史 DB 持久化**：经 `api/conversation.ts`（`/api/call/conversations` + `/messages`）每轮 user/assistant 消息落库，挂载时按"当前用户+场景"恢复最近一条会话（含 ai_session_id）。**call 场景转工单**有两条触发路径，两者都会触发 `refreshTasks()` 刷新右上角「历史工单」badge 红点（按角色仅统计本人 new/in_progress/pending），建单后留在摇人页（不再跳系统任务）：
  - ① 外层「转工单」按钮：`handleSubmitTicket` 调 `qaSubmit`→AI 模块生成工单并入库后调 `refreshTasks()`。
  - ② 对话内输入「转工单」等关键词：AI 后端在 `pipeline.run_stream` 自动调 `submit` 建单，SSE `result` 事件回传 `data.ticket`；`ChatPanel.send` 解析到 `result.ticket` 后置位标记，流式正常结束后调 `refreshTasks()`。

---

## 渲染与安全

- **`MarkdownRenderer.tsx`**：AI 流式返回内容的 GFM 解析 + 媒体渲染。`react-markdown` + `remark-gfm`（**已重新引入**，不要按旧文档删除）。管线：`stripCodeFences`（剥掉 LLM 把整段回复用 ``` 包裹、避免渲染成黑底代码块）→ `preprocessMediaUrls`（裸 URL→图片/视频）→ `react-markdown` 解析 → 自定义 `img/a/pre/code/table/p`；`AuthImage` 对同源/相对路径图片做 Bearer token 鉴权加载（blob URL）。外层 `MdErrorBoundary` + `simpleMarkdownToHtml` 兜底：react-markdown 异常时降级为简易 HTML，不白屏。
- **`SafeHtml.tsx`**：dompurify 清洗后渲染可信 HTML（工单详情/评论），用于非 AI 的用户内容。
- XSS：MarkdownRenderer 输出 React 虚拟 DOM，不叠 dompurify；SafeHtml 才用 dompurify。

---

## 认证架构（三层）

1. **API 客户端层** `src/api/client.ts`：`createRequest(baseUrl, serviceName)` 工厂；全局 token（`setToken`/`clearToken`/`initToken`）；GET 5 分钟缓存（`skipCache` 跳过）；401 自动刷新（pub-sub 防并发）；500+ 重试 2 次指数退避；30s 超时映射 408；`skipAuth` 跳过 token；统一 `ApiError`。
2. **Zustand 层** `src/stores/auth.ts`：`isLoggedIn/username/token/isAdmin/roles`；`checkLoginStatus`（启动恢复）、`login/logout/refreshAuthToken/fetchUserDetails`。`main.tsx` 启动时先 `checkUrlTokens()` 再 `checkLoginStatus()`——**必须在 `RouterProvider` 渲染前提取 URL token**：访问 `/p/app/call?token=...`（微信菜单链接，单层 `/app`）会命中旧路由 `<Navigate>` 重定向到 `/app/call` 而丢 query，若不在路由匹配前取出，AuthGuard 里的 `checkUrlTokens` 就再也拿不到 token；提前取出存 localStorage 后，带 token 直链可直接登录（会硬刷新一次去掉 URL 上的 token）。
3. **路由守卫层** `src/shared/utils/authGuard.tsx`：`AuthGuard` 包裹 `/app`；未登录时按 `WECHAT_CONFIG.loginEnabled` 决定跳转——**启用微信登录则跳微信 OAuth 授权页**（先尝试 `checkUrlTokens` 从 URL 恢复 token，再 `buildWechatAuthUrl` 构造 `open.weixin.qq.com` 授权链接，state 由 `buildStateFromPath()` 把**完整目标地址**（`origin + 部署前缀 + 路由路径`）做 base64url 编码，后端解码后原样回跳，避免丢失 `/p/app` 前缀）；否则跳 `/login` 账密页。`requireAdmin` 跳 `/no-permission`；`VITE_DISABLE_AUTH_GUARD=true` 可跳过（测试用）。登录页 `pages/Login.tsx` 在启用微信登录且非 `?debug=true` 时也会直接跳微信（`?debug=true` 保留账密表单，供后台人员登录）；`VITE_WECHAT_REDIRECT_URI` 显式覆盖回调地址（生产必须带 `/p/api` 前缀）。

---

## 后端与代理（`vite.config.ts` + `src/config/api.ts`）

后端拆为两个独立服务（与 `deploy/nginx/conf/conf.d/app_gateway.conf` 对齐）：
- **业务后端** `backend/main.py` @**8400**：`/api/auth|admin|tasks|call|wechat`
- **AI 服务** `ai/run.py` @**8401**：`/api/ai/*`（诊断/对话/记忆，含 SSE 流式）

| 服务 | BASE_URL | 用途 |
|------|----------|------|
| AUTH | `${API_ROOT}/auth` | 登录/注册/刷新/用户信息 |
| CALL | `${API_ROOT}/call` | AI 对话/会话/消息/我的任务（业务后端侧） |
| **AI** | `${API_ROOT}/ai` | 诊断 Agent / LLM 对话 / 会话记忆（**独立 AI 服务 @8401**） |
| TASKS | `${API_ROOT}/tasks` | 工单 CRUD/评论/附件/状态流转 |
| ADMIN | `${API_ROOT}/admin` | 项目/风险/日报/用户/角色/权限/资源 |

`API_ROOT` 由 `config/api.ts` 据 vite `base` 自动推导，**dev 与打包天然一致**：
- **dev**（`base='/'`）：`API_ROOT='/api'`。`vite.config.ts` 代理拆分——`/api/ai/*`→AI(8401)、其余 `/api/*`→业务后端(8400)；本地需同时启动 `backend/main.py` 与 `ai/run.py`。目标可用环境变量 `VITE_DEV_BACKEND_TARGET` / `VITE_DEV_AI_TARGET` 覆盖。
- **打包**（无 vite 代理，靠 nginx 按环境前缀分发）：`npm run build:test`（`base='/t/app/'`→`API_ROOT='/t/api'`）或 `build:prod`（`'/p/app/'`→`'/p/api'`）；nginx 最长前缀把 `/t/api/ai/*` 转给 AI、`/t/api/*` 转给业务后端，前端无需感知分流。

---

## 目录结构（关键文件）

```
src/
├── api/
│   ├── client.ts         # 通用 fetch 封装（token/缓存/刷新/重试）
│   └── ai.ts             # AI 模块（QA/Chat/Memory，SSE）
├── config/api.ts         # 五服务 BASE_URL
├── stores/
│   ├── auth.ts           # 认证状态
│   └── workbench.ts      # 三视角跨视图联动中枢 ★
├── router/index.tsx      # 路由（/app 工作台收口 + 旧路由重定向）★
├── shared/
│   ├── components/
│   │   ├── MainLayout.tsx       # 工作台外壳（底部 TabBar）★
│   │   ├── AdminLayout.tsx      # 后台外壳（Navbar + 抽屉菜单）
│   │   ├── ChatPanel.tsx        # 可复用 AI 对话面板（SSE/转工单）★
│   │   ├── MarkdownRenderer.tsx # GFM + 媒体/鉴权图片 ★
│   │   ├── SafeHtml.tsx / Pagination.tsx
│   ├── utils/{authGuard.tsx,url.ts,list.ts}
│   ├── constants/ticket.ts      # 工单状态/优先级/类型 中英文映射
│   └── styles/global.css
├── pages/
│   ├── Login.tsx / NoPermission.tsx
│   ├── call/{CallView,HistoryTickets,TicketDetailPage}.tsx
│   ├── tasks/{TasksView,TicketList,TicketDetail}.tsx
│   └── admin/  (AdminView + 17 个管理页 + components/FileUpload|JsonImport|QRCodeImport)
└── main.tsx / App.tsx
```

---

## 重要约定

1. **路径别名** `@/` → `src/`（vite + tsconfig 均配置）。
2. **导出** 统一 `export default function`；页面懒加载 `React.lazy` + `<Suspense>`。
3. **每个页面左上角返回按钮**：`<Navbar leftArrow onLeftClick={() => navigate(-1)} />`（TDesign `leftArrow` 须显式 `true`）。
4. **`AuthGuard` 仅包裹 `/app`**；`/login`、`/no-permission` 自身无 `AuthGuard`。但 `/login` 在 `VITE_WECHAT_LOGIN_ENABLED=true` 且非 `?debug=true` 时，会由 `Login` 组件直接跳微信 OAuth（与受守卫页行为一致），仅 `?debug=true` 才显示账密表单；`VITE_DISABLE_AUTH_GUARD=true` 时全站跳过守卫。
5. **lint**：`@typescript-eslint/no-explicit-any` 为 error（mock 组件用显式 props 类型）；`no-unused-vars` 为 warn（未用参数加 `_` 前缀）；`react-hooks/exhaustive-deps` 的 warning 属预期（useEffect 内调 request/fetchX 的固定写法，**勿自动改**）。以 `tsc` 编译结果为准，勿轻信 ESLint 未使用告警直接删。
6. **全局状态两个 store**：`auth`（认证）+ `workbench`（跨视角联动），不要再造第三个全局 store；页面级状态自管理。
7. **构建拆包**：`manualChunks` 已拆 `echarts/tdesign/react`；echarts 仍 >500kB（全量引入），如需瘦身改 `echarts/core` 按需注册（较深重构）。
8. 移动端优先：`index.html` 锁 `maximum-scale=1.0, user-scalable=no`。
9. 依赖增删前先 grep 确认；当前依赖含 `react-markdown`+`remark-gfm`（MarkdownRenderer 用）、`dompurify`（SafeHtml 用）。

---

## 维护历史

- **2026-07-14 — 全面清理与文档同步**（原 CODEBUDDY.md）
  - 移除 `react-markdown`/`@types/dompurify` 等未用依赖；补齐 `eslint.config.js`；修复 10 个 `no-explicit-any`；清理多处死代码；`vite.config.ts` 增 `manualChunks`。
  - 注：`react-markdown` 后因 MarkdownRenderer 需求**重新引入**，见下条。

- **2026-07-16 — 工作台架构重构 + AI 模块落地**（本次同步，对应提交 `f320dfd` 等）
  - **路由收口**：新增 `/app` 工作台（`MainLayout` 底部三 Tab），旧路由全部重定向；移除 HomePage 临时导航。
  - **新增 store**：`stores/workbench.ts`（三视角跨视图联动：ticketDraft / chatContext / tasksRefreshKey / selectedTicketId / goToTab）。
  - **新增 AI 模块**：`api/ai.ts`（QA 诊断 SSE / LLM 对话 / 会话记忆）+ `config/api.ts` 增 `AI` 服务。
  - **新增页面**：`call/CallView`、`call/HistoryTickets`、`tasks/TasksView`、`admin/AdminView`（入口卡片网格）、`admin/DailyReportManage`。
  - **新增组件**：`shared/components/ChatPanel`（可复用 AI 对话面板）、`MarkdownRenderer`（GFM+鉴权图片）、`Pagination`、`MainLayout`。
  - **重新引入** `react-markdown` + `remark-gfm`（供 MarkdownRenderer）。
  - **代理调整**：`vite.config.ts` 拆分 `/api/auth`、`/api/ai`（本地 AI 后端）与其余 `/api`（测试服务器）；target 切换机制见下条（已实现）。

- **2026-07-16 — vite 构建代理配置 + 项目文档与提交流程**（提交 `ebf2088`）
  - `vite.config.ts` 改为函数式 `defineConfig(({ command }) => ...)`：`serve`（`npm run dev`）→ 测试服务器 `http://8.152.219.181:8008`；`build`（`npm run build`）→ proxy target 置空。
  - `.gitignore` 新增 `frontend/项目文档.md`（本地分析文档不推送远程）。
  - 新建本 `项目文档.md`（已忽略，仅本地留存）；确立「帮我提交代码」标准工作流（见文首）。

- **2026-07-16 — AI 诊断对话模块与 Markdown 渲染**（提交 `38fcb6a`，"全部提交"剩余 WIP）
  - 新增 `api/ai.ts`（QA 诊断 SSE / LLM 对话 / 会话记忆）、`shared/components/MarkdownRenderer`（GFM + 鉴权图片/视频渲染）。
  - 重新引入 `react-markdown` + `remark-gfm`（`package.json` / `package-lock.json`）；`config/api.ts` 增 `AI` 服务配置。
  - AI 对话相关调整：`ChatPanel` / `AIChat` / `ConversationList`；`global.css` 样式扩充；`authGuard` / `workbench` / `Login` / `UserManage` 小幅调整。

- **2026-07-18 — 本地联调打通 + AI 回复渲染修复（代码块问题）**
  - **`vite.config.ts`**：`TEST_API_TARGET` 改为 `http://localhost:8400`，前端 `/api/*` 统一走**本地后端**（曾指向远程测试服 `125.122.97.107:8801` / 空）。改 `optimizeDeps` 后须重启 dev server。
  - **`MarkdownRenderer.tsx`**：保持 `react-markdown` + `remark-gfm`（中途试过 `marked` 方案已弃用，`marked` 现为未用依赖、可清理）；新增 `stripCodeFences` 预处理——DeepSeek 会把整段回复用 ``` 包裹，导致 react-markdown 渲染成黑底代码块、`**加粗**` 不解析；保留 `AuthImage` 鉴权图片；补 `MdErrorBoundary` + `simpleMarkdownToHtml` 兜底。
  - **后端配套**（详见 `docs/SETUP.md`、`backend/CODEBASE_OVERVIEW.md`）：`app/core/db.py` 异步驱动改为「优先 `asyncmy`，缺失回退 `aiomysql`」（Py3.14 下 asyncmy 无法编译）；本地用专用 MySQL 容器 `openrobot-mysql`(8.0.46)@**3307**，`.env` 的 `DATABASE_URL` 指向 3307（既有共享 MySQL 是 8.0.13，有 `DEFAULT(now())` 建 index bug，不可用）。

- **2026-07-18 — 摇人「历史工单」详情页 + 对话历史 DB 持久化**
  - **Task 1 历史工单详情页**：新增 `pages/call/TicketDetailPage.tsx`（全屏，合并旧 `TicketDetail` + `TasksView` 弹层能力：状态流转/催办/修改/讨论/上报/评论），路由 `/app/call/ticket/:id`。`HistoryTickets` 点击改为进该页（不再跳系统任务），行内增加 description 梗概。`ChatPanel` 转单后直接 POST 建单 + `refreshTasks()`，**留在摇人页**。
  - **Task 2 对话历史持久化（DB）**：新增 `api/conversation.ts`（CALL 会话/消息客户端；场景映射 call→`chat`、tasks→`consultation`——后端 `SceneType` 无 `task_assist`）；`ChatPanel` 每轮消息落 `conversations/messages` 表，挂载时恢复最近会话；`ai_session_id` 存会话 `metadata_`。
  - **后端**：`call/api/conversation.py` 的 `create_conversation` 改为从 token 取 `user_id`（前端只持有 username，须与列表查询的 `current_user.id` 对齐）。
  - 备注：后端 `TicketResponse` 无 `ai_summary` 字段（TasksView 那个 AI 摘要实际一直为空）；旧 `tasks/TicketDetail.tsx` 已删除（无引用）。

- **2026-07-18 — 后端/AI 拆分双服务，dev 与打包接口全通**
  - **背景**：后端拆为**业务后端** `backend/main.py`@8400 与**独立 AI 服务** `ai/run.py`@8401（路由 `/api/ai/*`，`backend/app/__init__.py` 已移除 AI 路由挂载）。nginx（`deploy/nginx/conf/conf.d/app_gateway.conf`）按环境前缀 `/t`、`/p` 分发：`.../api/ai/*`→AI、其余 `.../api/*`→业务后端。
  - **`config/api.ts`**：新增 `API_ROOT`，由 vite `base` 推导——dev `base='/'`→`/api`；构建 `base='/t/app/'`→`/t/api`、`'/p/app/'`→`/p/api`。所有服务 BASE_URL 改用 `${API_ROOT}/xxx`，dev/打包一致。
  - **`vite.config.ts`**：dev 代理拆两条——`/api/ai`→AI(8401)、`/api`→业务后端(8400)，目标可 `VITE_DEV_BACKEND_TARGET`/`VITE_DEV_AI_TARGET` 覆盖；移除原单一 `TEST_API_TARGET`。`base` 可 `VITE_APP_BASE` 覆盖。
  - **`package.json`**：新增 `build:test`（`--base=/t/app/`）、`build:prod`（`--base=/p/app/`），产物分别放 nginx `html/test/`、`html/prod/`。已实测构建产物资源走 `/t/app/`、API 走 `/t/api`。
  - **本地 dev 需同时启动**：`backend/main.py`(8400) + `ai/run.py`(8401)。详见 `docs/SETUP.md`。

- **2026-07-18 — 摇人历史工单改走 AI 模块接口（修正）**
  - **背景**：此前把历史工单错接到业务后端 `/api/tasks`（Task 表/状态机）。AI 诊断生成的工单其实属 AI 模块，按 **session** 组织：`/api/ai/qa/submit` 生成工单（写 MySQL `tickets` 表 + 入待派单 Redis set）、`/api/ai/memory/tickets` 列待派单 session_id、`/api/ai/qa/ticket?session_id=` 取工单详情、`/api/ai/qa/ticket/ack` 派单回执。
  - **`HistoryTickets.tsx`**：列表改走 `memoryTickets()`（session_id 列表）+ 逐条 `qaGetTicket()` 取梗概（标题/类型/优先级/描述）；点击进 `/app/call/ticket/:sessionId`。
  - **`TicketDetailPage.tsx`**：重写为 AI 工单视图——title/type/priority/status/contact/description + `diagnosis`(概述/推测/已排除/轮数) + 类型专属字段（problem: 位置/型号/故障码；bug: 严重程度/复现步骤…）；操作改为「确认派单」(`qaTicketAck`)，移除原 Task 的催办/修改/状态流转/评论。
  - **`ChatPanel.handleSubmitTicket`**：移除错误的 `POST /api/tasks` 建单，改为只调 `qaSubmit(sessionId)`（AI 模块自建工单+入库）后 `refreshTasks()`，并清理无用 import。
  - **以此条为准**，修正 2026-07-18「摇人历史工单详情页」条目里"POST 建单 + /api/tasks 详情"的旧描述。
  - 注意：`memory/tickets` 当前是**全局**待派单列表（AI 模块无用户鉴权/分域），多用户场景需后端补 user 维度。

- **2026-07-18 — 前端死代码清理（第 1 档：纯删除）**
  - 删除 3 个无引用的 call 页：`pages/call/AIChat.tsx`、`ConversationList.tsx`、`NewTicket.tsx`（router 与全工程均无 import；旧路由 `/call/ai-chat`、`/call/new-ticket` 仍保留 `<Navigate>` 向后兼容）。
  - `api/ai.ts` 移除从未接线的接口导出：`qaAsk`、`qaHealth`、`chatSend`/`chatSendStream`、`memoryHistory`/`memoryTickets`/`memoryClear`/`memoryClearAll` 及其类型（`QAAskResponse`/`ChatRequest`/`ChatResponse`/`HistoryResponse`/`TicketsResponse`），以及随之失去引用的 `aiDelete` helper。保留 `qaAskStream/qaSubmit/qaGetTicket/qaListTickets/qaTicketAck/qaUpload` + session helpers。文件 217 → ~120 行。

- **2026-07-20 — 微信网页授权登录（OAuth）落地**
  - 前端 `config/wechat.ts` 新增 `redirectUri`（`VITE_WECHAT_REDIRECT_URI`，显式覆盖回调地址）；`shared/utils/url.ts` 的 `buildWechatAuthUrl` 优先用 `redirectUri`，否则回退 `origin + redirectPath`。
  - 修复 `authGuard`：`state` 仅用 `pathname`（原 `pathname+search` 会导致带 query 页面 token 拼接错乱）；并统一 `useAuthGuard` 也走 `redirectUnauthenticated`（原硬编码跳 `/login`）。
  - `pages/Login.tsx`：启用微信登录且非 `?debug=true` 时直接跳微信 OAuth（与受守卫页一致），`?debug=true` 保留账密表单供后台人员登录；已登录则回来源页。
  - `vite-env.d.ts` 增加 `VITE_WECHAT_REDIRECT_URI` 类型；`.env.production/.env.staging/.env.local` 补充微信配置。
  - 修复 `.gitignore` 误忽略 `.env.production/.env.staging`（导致生产构建读不到开关、跳账密页）；`deploy/nginx/conf/conf.d/app_gateway.conf` 增加微信校验文件放行（`location ~ ^/MP_verify_.*\.txt$`）。
  - 生产域名 `usp.ep-zl.com`，回调 `https://usp.ep-zl.com/p/api/wechat/callback`，生产 AppID `wx7c6fcd3acf3db8bd`。详见 `docs/PRODUCT/WECHAT.md`。

- **2026-07-20 — 微信授权 `state` 改为携带完整目标地址（base64url）**
  - 问题：旧 `state` 仅用 pathname 的 `/`→`0` 编码（如 `0app0admin0wechat`），后端 `/wechat/callback` 用回调请求的 `scheme://netloc` 重拼回跳地址，**丢失 `/p/app` 部署前缀**，导致生产回跳 404。
  - 前端 `shared/utils/url.ts` 新增 `encodeWechatState()`（完整 URL → base64url）与 `buildStateFromPath()`（拼 `origin + BASE_URL + 路由路径` 后编码）；`authGuard.tsx` 与 `Login.tsx` 改用 `buildStateFromPath()` 生成 `state`。
  - 后端 `app/wechat/api/wechat.py` 引入 `base64` 与 `resolve_callback_target()`：优先按 base64url 解码 `state`，以 `http(s)://` 开头则**原样回跳**；无法解码则兼容旧 `0`→`/` 路径格式兜底。

- **2026-07-20 — 历史工单列表接口迁移到 `/api/ai/memory/tickets/all`**
  - 后端接口变更：摇人「历史工单」列表由 `GET /api/ai/qa/tickets` 迁移到 `GET /api/ai/memory/tickets/all`，新增可选筛选参数 `status`（pending/dispatched/in_progress/resolved/closed）、`type`（problem/bug/feature/support/other）；`skip`/`limit` 不变（limit ≤200）。
  - 前端 `api/ai.ts` 的 `qaListTickets` 改打 `/memory/tickets/all`，签名扩展为 `(skip, limit, filters?: AiTicketListFilters)`，新增导出类型 `AiTicketListFilters`；`HistoryTickets.tsx` 调用处（`qaListTickets(0, 200)`，取全部不筛选）不变，仅同步顶部注释路径。
  - 响应 `{code, data:{total, skip, limit, items}}`；`AiTicketBrief` 按后端 Ticket 表对齐——移除 `created_by`，补 `contact/location/robot_type/fault_code/severity/attachments/diagnosis/updated_at`。`HistoryTickets` 列表渲染字段不变（仍用 session_id/type/title/description/priority/created_at）。

- **2026-07-24 — 修复「历史工单」badge 红点计数不实时刷新**
  - 问题：对话中输入「转工单」等关键词由 AI 后端自动建单后，右上角 badge 不 +1；但外层「转工单」按钮建单后会正常刷新，两条路径口径不一致。根因：`pipeline.run_stream` 在 AI 自动建单时已通过 SSE `result` 事件回传 `data.ticket`（`pipeline.py` 赋值并 yield、`router.py` 透传），但 `ChatPanel.send` 解析 `result` 事件时只判断 `data.root_cause_analysis`（任务 Agent 专属），完全忽略了 `data.ticket`，从不调用 `refreshTasks()`。
  - 前端 `ChatPanel.send`：新增 `ticketCreatedThisTurn` 标记；`result` 事件命中 `data.ticket` 时置位，流式正常结束后调 `refreshTasks()`，使「对话内 AI 自动建单」与外「转工单」按钮两条路径都触发 badge 重新计数。后端无需改动（SSE 已回传 ticket），计数口径维持 new/in_progress/pending 不变。

- **2026-07-24 — 摇人「历史工单」列表卡片布局重构（文字更多、更简洁）**
  - **`HistoryTickets.tsx`**：卡片由「横向单行（圆点+梗概+优先级文字+日期+3 按钮）」改为**纵向三行**——① 标题行：优先级圆点 + 类型标签（`history-row__type` 新增样式）+ 标题（2 行截断）+ 优先级文字（着色）+ 日期；② 描述行：去掉 `slice(0, 40)` 截断，全文渲染、CSS 2 行截断；③ 底部行：左侧**状态徽标**（新增 `STATUS_META`：new/pending/dispatched/in_progress/resolved/canceled/closed → 中文标签+前景/底色，替代原来与圆点重复的优先级文字），右侧「催办/上报/撤回」操作按钮不变。
  - **`global.css`**：`.history-row` 改 `flex-direction: column`、移除 `min-height: 112px`（卡片高度由内容决定，列表更紧凑）；`.history-row__title` 改 2 行 `-webkit-line-clamp`；`.history-row__summary` 由单行 `nowrap` 改 2 行截断；`.history-row__status` 改徽标样式（圆角胶囊，颜色内联）；新增 `.history-row__top/__bottom/__type/__priority`；删除已无引用的 `.history-row__main` 系列规则；`.history-row__actions` 移除 `margin-left`（底部行 `space-between` 布局）。
  - 效果：标题/描述可见文字量约提升一倍，状态/优先级信息分层展示（圆点=优先级、徽标=状态），操作按钮不再挤压文本宽度。

- **2026-07-27 — 新增「个人信息中心」：三模块统一右上角用户入口（头像上传 + 登出）**
  - **新组件 `shared/components/UserAvatarMenu.tsx`**：Navbar 右上角触发器（有头像显示 28px 圆形缩略图，无头像显示 tdesign `UserCircleIcon`），点击从顶部 `Popup` 弹出个人信息面板——56px 大头像（右下角相机角标，点击调起文件选择上传）、用户名称（`name || username`）、用户 ID（`username`）、底部「退出登录」红色描边按钮（`LogoutIcon`）。
  - 头像上传流程与 `UserProfile.tsx` 一致：`uploadAvatar(file, username)`（资源管理中心，限 JPG/PNG/WebP/GIF ≤5MB）→ `updateMyProfile(username, {avatar_resource_id})` 绑定 → `setProfile` 更新 store；面板打开时 `getMyProfile()` 拉取最新资料。登出调 `authStore.logout()`（清 token/localStorage）后 `navigate('/login')`。
  - **三模块集成**：`CallView`（我要摇人，历史工单按钮右侧）、`TasksView`（系统任务，同步按钮右侧）均将 Navbar `right` 改为 flex 横排追加 `<UserAvatarMenu />`；`Dashboard`（后台管理首页）原 👤 emoji + 跳 `/admin/profile` 直接替换为 `<UserAvatarMenu />`；`AdminLayout`（后台二级页）Navbar 新增 `right={<UserAvatarMenu />}`，全站一致。
  - **`global.css`**：新增 `.user-avatar-menu__trigger/__thumb` 与 `.user-panel` 系列样式（面板圆角底边、头像相机角标、safe-area 适配）。
  - 纯前端改动，后端复用现有 `/api/auth/me`、`/api/admin/users/{username}`、资源管理中心上传接口。校验：`tsc --noEmit` 通过、ESLint 0 error。

- **2026-07-27 — 个人信息中心支持姓名展示与编辑**
  - **`UserAvatarMenu.tsx`**：面板姓名行新增铅笔编辑按钮（`EditIcon`），点击切换为编辑态——`Input` 输入框（限 20 字、clearable、保存中禁用）+ 蓝色确认按钮（`CheckIcon`）+ 灰色取消按钮（`CloseIcon`）；保存调 `updateMyProfile(username, { name })` 成功後 `setProfile({ name })` 同步 Zustand store，姓名未变更时直接退出编辑态不发请求，空值 Toast 拦截。
  - **`global.css`**：新增 `.user-panel__name-row`（名字+编辑按钮横排）、`.user-panel__name-edit`（输入框+操作按钮横排）、`.user-panel__name-edit-btn`（铅笔按钮）、`.user-panel__name-action`（确认/取消按钮，`--confirm` 变体为主色填充）。
  - 校验：`tsc --noEmit` 通过、ESLint 0 error。

- **2026-07-27 — 修复头像上传后报错、不回显、Navbar 图标不替换**
  - 现象：头像上传成功后资源 `resource_status` 卡在 `processing`，前端 `<img>` 请求 `.../resources/{id}/download` 被后端以 403「资源不可用」拒绝（因 `Resource.is_available` 仅 `AVAILABLE` 为真），导致头像既报错也无法回显，Navbar 右上角图标保持默认 `UserCircleIcon`。
  - 根因：`resource_service.create_resource` 上传完成后把资源状态置为 `ResourceStatus.PROCESSING`，而项目并无异步转码任务将 `PROCESSING → AVAILABLE`，资源永久卡死；下载接口据此拒绝服务。
  - 后端修复（`resource_manager/services/resource_service.py`）：头像/图片上传为**同步流程**，MinIO 写入成功即文件就绪，将 `create_resource` 的资源状态由 `PROCESSING` 改为 `ResourceStatus.AVAILABLE`，下载接口即可正常返回图片。资源管理中心此前卡在 processing 的资源也随之恢复可用。
  - 前端无需改动：上传成功后 `uploadAvatar` → `updateMyProfile(username, {avatar_resource_id})` → `setProfile({ avatarResourceId })` 已写入 Zustand store；Navbar 的 `UserAvatarMenu`（AdminLayout `right`）与 `UserProfile` 页面均订阅 `avatarResourceId`，状态变化即触发重渲染，头像与右上角图标实时刷新。
  - 校验：上传新头像后资源状态变为 `available`，`/download` 返回 200，Navbar 缩略图与个人信息面板头像即时替换。

- **2026-07-27 — 头像持久化（刷新/重登不丢失）+ 点击查看全屏大图**
  - 现象：上传后当次回显正常，但**刷新页面或重新登录后头像消失**（Navbar 与面板都回到默认 `UserCircleIcon`）。根因：头像仅存于 Zustand 内存 store（`uploadAvatar` → `setProfile`），刷新/重登后 store 重置；且 `fetchUserDetails` 此前在应用代码中从未被调用，登录/启动恢复登录态（`checkLoginStatus`）时并不从后端回填 `avatar_resource_id` / `name` / `roles`。后端 `/auth/me`、`/users/{username}/detail` 与数据层 `get_user_with_roles` 均正确返回 `avatar_resource_id`，故缺失的是前端回填。
  - 修复「持久化」：
    - `stores/auth.ts` 的 `login`：`set` 登录态后 `void get().fetchUserDetails(user, access_token)` 异步回填资料，登录即加载头像。
    - `main.tsx`：在 `checkLoginStatus()` 同步恢复 `token/username` 后，若二者均存在则 `void useAuthStore.getState().fetchUserDetails(username, token)` 异步回填，刷新后头像持续展示。
    - `fetchUserDetails` 内部已 `setApiToken` 并写入 store 的 `avatarResourceId/name/isAdmin/roles`，Navbar（`UserAvatarMenu`）与 `UserProfile` 订阅即实时刷新；接口失败静默兜底，不影响登录态。
  - 新增「全屏预览」（`shared/components/UserAvatarMenu.tsx` + `global.css`）：个人信息面板中**点击已上传的头像图片**打开全屏遮罩预览（`.avatar-fullscreen` 黑色半透明、点击遮罩关闭、图片 `object-fit: contain` 自适应）；**点击右下角相机角标**仍触发上传（`stopPropagation` 避免误触预览）；无头像时点击头像区仍是上传入口。Navbar 右上角入口保持「点击展开个人中心面板」语义不变。
  - 校验：`tsc --noEmit` 通过；刷新/重登后 Navbar 头像保留，面板内点头像弹出全屏大图、点头像角标上传。

- **2026-07-27 — 修复「用户中心页（/admin/profile）头像不显示」**
  - 现象：从 Navbar 右上角入口进入用户中心页后头像不显示（显示兜底图标），而 Navbar、个人信息面板等其它位置均正常。
  - 根因（两处叠加）：① 该页此前用 tdesign `<Avatar image={...}>` 渲染头像，其内部走 tdesign `<Image>` 组件，渲染行为与原生 `<img>` 不一致；② 页面挂载 `useEffect` 调 `getMyProfile()`（`/auth/me`）后执行 `setProfile({ ..., avatarResourceId: profile.avatar_resource_id ?? null })`，用接口返回值覆盖了 Zustand store 中本已正确的头像 id，一旦该字段缺失/为 `null` 即把头像弄丢（且会连累 Navbar）。后端 `/auth/me` 与 `/users/{username}/detail` 同源、均正确返回 `avatar_resource_id`，Navbar 已能显示，故数据源无问题。
  - 修复（`pages/admin/UserProfile.tsx`）：头像渲染改与 Navbar 一致，用原生 `<img src={avatarUrl(avatarResourceId)}>`（80px 圆形 `object-fit: cover`），并加 `onError` 兜底回退到 `UserCircleIcon`；`useEffect` 仅刷新昵称 `setProfile({ name })`，**不再**用 `/auth/me` 的 `avatar_resource_id` 覆盖 store（store 已由登录/刷新时的 `fetchUserDetails` 回填，同源无需重复拉取）；上传新头像成功后 `setAvatarError(false)` 确保即时回显。移除不再使用的 tdesign `Avatar` 导入，新增 `tdesign-icons-react` 的 `UserCircleIcon`。
  - 校验：`tsc --noEmit` 与 ESLint 0 error；进入用户中心页头像正常显示，上传后两处（Navbar + 该页）即时更新。

- **2026-07-27 — 修复「点击右上角头像弹出面板后，头像闪现一下又变回图标」**
  - 现象：点击 Navbar 右上角头像打开个人信息面板（`UserAvatarMenu` 的 `Popup`），头像先正常显示一下，随即变回 `UserCircleIcon` 默认图标。
  - 入口核实：全站右上角入口（`Dashboard`/`AdminLayout`/`CallView`/`TasksView` 的 Navbar `right`）均为 `<UserAvatarMenu />`，其触发器点击只 `setVisible(true)` 弹出顶部 `Popup`，**无任何代码导航到 `/admin/profile`**（搜索 `navigate/to` + `profile` 为 0）。故用户所说的「用户中心页面」实为该 Popup 面板，上一条对 `UserProfile.tsx` 的改动保留备用（路由仍存在）。
  - 根因（两层）：①前端 `UserAvatarMenu` 的 `useEffect`（`visible` 变 true 时）调 `getMyProfile()`（`/auth/me`，`skipCache:true` 实时请求）后执行 `setProfile({ avatarResourceId: profile.avatar_resource_id ?? null })`，用接口返回值覆盖 store 中本已正确的头像 id；②**后端 `/api/auth/me` 返回的 `avatar_resource_id` 实为 `null`**——经 TestClient + DB 实查定位：`/api/auth/me` 命中的是 `app/core/auth_routes.py`（挂在 `/api/auth`），其调用的 `app/core/auth_service.py:AuthService.get_user_info` 返回的 dict **漏了 `avatar_resource_id` 字段**（与 `app/modules/admin/services/auth_service.py` 的同名方法不同步，后者有该字段）；而 `/users/{username}/detail`（`admin/api/users.py`）直接用 `get_user_with_roles` 并显式传 `avatar_resource_id`，故返回正确值（DB 实为 4）。时序：Popup 打开瞬间用 store 现值渲染 `<img>`（闪现）→ `/auth/me` 返回 `null` → 覆盖清空 store → 切回 `UserCircleIcon`。Popup 内 `<img>` 无 `onError`，「变图标」只能是 store 被清空。
  - 修复（前端 `shared/components/UserAvatarMenu.tsx`）：`useEffect` 改为**仅刷新昵称** `setProfile({ name: profile.name || undefined })`，**不再**用 `/auth/me` 的 `avatar_resource_id` 覆盖 store——头像 id 权威来源单一化为 `fetchUserDetails`（`/users/{username}/detail`，登录/刷新回填）与上传后 `setProfile`，从根上消除「返回 null 清空头像」的脆弱性。
  - 修复（后端 `app/core/auth_service.py`）：`get_user_info` 返回 dict 补上 `"avatar_resource_id": user.get('avatar_resource_id')`，与 `admin/services/auth_service.py` 对齐，使 `/api/auth/me` 正确返回头像 id（TestClient 验证：修复前 `null`，修复后 `4`）。
  - 后端核实：`get_user_with_roles`（`permission_service.py:281`）正确返回 `avatar_resource_id`；`update_user`（`identity_service.py:119-123`）经 `setattr`+`commit` 持久化该列；DB 实查 `admin.avatar_resource_id=4`。问题仅在 `core/auth_service.get_user_info` 漏字段，非 DB 持久化问题。
  - 校验：`tsc --noEmit` 与 ESLint 0 error；TestClient `/api/auth/me` 返回 `avatar_resource_id=4`；打开面板头像不再消失。

- **2026-07-27 — rebase 合并修复：恢复 UserProfile 被丢弃的登出功能与 tdesign 导入**
  - 背景：本地 `develop` 相对 `origin/develop` 分叉，对修复提交 `beff059` 执行 `git rebase origin/develop` 时，`frontend/src/pages/admin/UserProfile.tsx` 在 import 区产生冲突。解决冲突时误采用 theirs（`beff059`）版本，导致 HEAD 侧引入的登出能力（`useNavigate` 路由跳转、`logout` 退出登录）以及 `tdesign-mobile-react` 组件（`Input`/`Button`/`Dialog`/`Upload`/`Toast`）的导入全部丢失；而文件仍引用这些符号，最终重放后的提交（`5e3e0d3`）实际**编译失败**。
  - 修复（`pages/admin/UserProfile.tsx`）：补齐 `import { useNavigate } from 'react-router-dom'` 与 `import { Input, Button, Dialog, Upload, Toast } from 'tdesign-mobile-react'`；组件内补回 `const navigate = useNavigate();` 与 store 解构中的 `logout`；保留「`useEffect` 仅刷新昵称、不覆盖 `avatarResourceId`」的修复策略；`handleLogout` 的 `logout()` + `navigate('/login')` 路由登出能力恢复。
  - 校验：`tsc --noEmit` 与 ESLint 0 error。

- **2026-07-28 — 修复「退出登录后自动跳回『我要摇人』（/call）」**
  - 现象：个人中心（`/admin/profile`）或右上角头像面板的「退出登录」点击后，先闪现登录界面，随后又被自动带回「我要摇人」（工作台首页 /call），登出未真正生效。
  - 根因：① `logout()`（stores/auth.ts）本身清除 token 与 localStorage 正确；②但两处 `handleLogout`（`pages/admin/UserProfile.tsx`、`shared/components/UserAvatarMenu.tsx`）只 `navigate('/login', { replace: true })`，**未带任何标记**，Login 页 `from` 默认即 `/call`；③`Login.tsx` 的 `useEffect` 在微信登录启用（`VITE_WECHAT_LOGIN_ENABLED=true`，生产环境）时，未登录即 `window.location.href = buildWechatAuthUrl(...)` **自动跳微信 OAuth**，回跳用 code 重新换发 token，于是「登出」瞬间又被自动登录，跳回 `/call`——表现为登录界面一闪后回到原页面。
  - 修复（stores/auth.ts 无需改；三处前端）：两处 `handleLogout` 登出跳转改为 `navigate('/login?reason=logout', { replace: true })`；`Login.tsx` 读取 `reason` 参数，`useEffect` 增加分支——`reason === 'logout'` 时**不再自动跳微信 OAuth**，停留在登录页；并在账密登录按钮下方新增「使用微信登录」按钮（仅微信启用时显示），点击才手动触发 `buildWechatAuthUrl` 重新登录。同时 `isLoggedIn && reason !== 'logout'` 才执行「已登录回跳来源页」，防御极端时序下误跳。
  - 效果：首次访问未登录仍自动跳微信（保持原 UX）；退出登录后停在登录页，由用户手动选择账密或微信重新登录，不再被自动登录带回原页面。
  - 校验：`tsc --noEmit` 与 ESLint 0 error（针对 Login.tsx / UserAvatarMenu.tsx / UserProfile.tsx）。

- **2026-07-28 — 修复「登出当下仍跳回摇人页」（reason=logout 仅挡一半，client.ts 401 抢跳转）**
  - 现象：上一条 `reason=logout` 修复后，账密环境（`VITE_WECHAT_LOGIN_ENABLED=false`）登出能停登录页，但**微信环境（生产）登出当下仍跳回摇人页**。
  - 根因：`logout()` 清空 token 后，`MainLayout`/`CallView` 的在途请求/轮询带旧 token 收到 401 → `client.ts` 401 分支触发 `refreshTokenRequest()` → refresh_token 已被清 → 刷新失败 → catch 分支 `window.location.href = buildWechatAuthUrl(...)` **整页跳微信静默授权**（snsapi_base openid 固定）→ 自动登录回跳 `/call`，**覆盖了** SPA 的 `navigate('/login?reason=logout')`。Login 页的 `reason` 保护只挡得住"从 Login 页发起"的微信跳转，挡不住 `client.ts` 的整页抢跳转。
  - 修复（`api/client.ts` + `stores/auth.ts`，临时退出语义）：新增模块级 `loggingOut` 标记（`setLoggingOut` 导出）——`logout()` 开头置 true、`login()` 开头置 false；`client.ts` 401 刷新失败的 catch 分支在 `loggingOut===true` 时**放弃抢跳转**（仅 throw），由调用方 `navigate('/login?reason=logout')` 把页面停在登录页。标记随 webview 重载复位（false），符合"临时退出"语义：登出当下停登录页，重开仍可自动登录。
  - 效果：微信环境登出当下不再跳回摇人页，停在登录页；点「使用微信登录」或账密登录后 `login()` 清标记，恢复正常 401 自动刷新/跳转逻辑。
  - **二次修复（同日）**：首次修复后微信端登出仍"停几秒又跳回摇人页"。根因是 AI 模块请求（`api/ai.ts` 的 `fetchWithAuth`）401 走 `shared/utils/session.ts` 的 `kickToLogin`，**不经过 `client.ts`**，未被 `loggingOut` 挡住：`kickToLogin` 600ms 后 `window.location.assign('/login?from=...')` 整页跳转（无 `reason=logout`），整页重载使 `loggingOut` 复位，Login 页因 reason 非 logout 再跳微信静默授权=自动登录回 `/call`。修复：`client.ts` 导出 `isLoggingOut()`；`session.ts` 的 `kickToLogin` 开头 `if (isLoggingOut()) return;`——登出流程中不抢跳转，由调用方 `navigate('/login?reason=logout')` 停在登录页。至此所有整页跳转点（`client.ts`/`session.ts`/`authGuard`/`Login`）均在登出流程中受控。
  - 同源隐患（未在本次范围）：`session.ts` session 失效跳 `/login?from=...` 无 reason；后端无 `/auth/logout`，token 不吊销。

- **2026-07-28 — 前端接入「转工单二次确认」全链路（prepare → 弹窗 → confirm）**
  - 背景：AI 侧已新增二次确认接口（`ai/api/router.py` 的 `POST /qa/ticket/prepare`、`POST /qa/ticket/confirm`、`GET /qa/ticket/draft`，pipeline 的 `prepare_ticket`/`confirm_submit`/`get_draft` + `_check_required_fields` 必填校验），但前端 `ChatPanel` 仍走旧的 `qaSubmit`（`/qa/submit` 一步式），未接新流程。
  - 改动：
    - `api/ai.ts`：新增 `TicketDraft` 类型 + `qaPrepareTicket`/`qaConfirmTicket`/`qaGetDraft` 三个封装；移除 `ChatPanel` 对 `qaSubmit` 的依赖。
    - `shared/components/ChatPanel.tsx`：`handleSubmitTicket` 改为先调 `prepare` 生成草稿 → 弹出确认弹窗（展示类型/优先级标签，可编辑标题/描述/优先级/联系人，`type=problem` 时必填项目/现场名称）→ 用户确认后调 `confirm(overrides)` 入库 → `refreshTasks()` 刷新历史工单。新增 `ticketConfirm` 状态、`draftField`/`setDraftField` 读写 helper、`handleConfirmTicket`、`TICKET_TYPE_LABEL` 常量。
    - `shared/styles/global.css`：新增 `.ticket-confirm` 系列样式。
  - 必填校验双重保险：前端 `handleConfirmTicket` 对 problem 类型校验 project 非空，后端 `confirm_submit` 再次 `_check_required_fields` 校验，缺失返回 `{code:1, missing_fields}`。
  - 全链路契约验证：`project` 经 `ticket_dict_to_task_fields` 落 `Task.project_name`、`task_to_dict` 反向读回；prepare 返回 `{stage,draft,missing_fields,prompt}`、confirm 返回 `{ticket,db_id,notice}`，前端按此消费。
  - 降级：prepare 后若 Redis 丢 memory 导致 confirm 时无 `ticket_draft`，后端返回 `{code:1, message:"没有待确认的工单草稿"}`，前端 Toast 提示重试。
  - **同日补充 — 项目必选（所有类型）**：草稿确认页"绑定项目"改为所有工单类型必填（原仅 `problem` 类型）。未填写项目时：确认按钮 `disabled` + 输入框下方红色提示"项目为必选项，未绑定项目无法提交"；`handleConfirmTicket` 所有类型均校验 project_id 非空并 `overrides={project, project_id}` 传入。`project` 经 `ticket_dict_to_task_fields` 落 `Task.project_name`、`project_id` 落 `Task.project_id`。后端 `_check_required_fields`（AI 侧）已同步为所有类型均校验 `project_id`（优先）/ `project`（回退），缺失返回 `{code:1, missing_fields:["project"], prompt:"请先选择/填写绑定项目..."}`，与前端项目必选规则对齐（既在对话框确认 API 也在聊天 ack 自动提交路径生效）。
  - **同日再补充 — 项目改为下拉选择 + 模糊搜索 + project_id 绑定**：文本输入改为 `ProjectSelect` 下拉选择器，从全量项目接口 `GET /api/admin/projects/?keyword=&include_analysis=false`（仅需登录 token，不查角色）拉取项目列表，支持按名称/编码模糊搜索。选中项目后绑定 `project_code`→`project_id`、`name`→`project_name`，`overrides` 同时传两者。校验改为 `project_id` 必填（未选项目禁用确认按钮 + 拒绝提示）。`ai/core/task_adapter.py` 的 `ticket_dict_to_task_fields`/`task_to_dict` 增加 `project_id` 映射，让绑定关系真正落 `Task.project_id` 列。新增 `api/projects.ts`、`shared/components/ProjectSelect.tsx`（复用 UserSelect 浮层结构与样式）。

- **2026-07-28 — 转工单全链路实测 + 补齐后端 project_id 校验对齐**
  - 全链路实测结论：前端 `prepare` → 弹窗（`ProjectSelect` 下拉模糊搜索、`project_id` 必填、未绑定禁用确认按钮 + 拒绝提示）→ `confirm(overrides={project, project_id})` → `task_adapter.ticket_dict_to_task_fields` 落 `Task.project_id` / `Task.project_name` → 历史工单 `task_to_dict` 读回，契约全部对齐；`npx tsc --noEmit` 0 error。
  - 修复缺口：后端 `pipeline._check_required_fields` 原仅 `type=problem` 校验 `project`（名称），与「所有类型项目必填、以 `project_id` 绑定」需求及前端兜底不一致；改为所有类型校验 `project_id`（优先）/ `project`（回退），缺失返回 `{code:1, missing_fields:["project"], prompt:"请先选择/填写绑定项目..."}`。对话框确认 API（`confirm_submit`）与聊天 ack 自动提交（line 597/1375 两处 `_build_ticket` 后校验）两条路径均强制绑定项目。

- **2026-07-28 — 绑定项目数据源切换为 AI wecom 接口（以 /api/ai/wecom/projects 为准）**
  - 背景：用户指定工单绑定项目的项目列表应以 `GET /api/ai/wecom/projects`（AI 服务，企业微信 Smartsheet 全量拉取）为准；原用 `GET /api/admin/projects/`（backend 经 wecom 同步后的镜像）。两者同源（均来自企业微信 USP 项目表）但服务/字段形态不同，按"以这个为准"切换。
  - 改动：`api/projects.ts` 的 `getProjects` 改调 `AI.BASE_URL + /wecom/projects`，解析 `{code, data:{records:[{record_id, values:{...}}]}}`；按 backend `wecom/adapter.map_wecom_record_to_project` 映射——`项目编号→project_code`、`项目名称→name`、`项目生命周期→status`，并仅保留 `是否承接=="是"` 的项目（与 admin 仅含承接项目的行为一致）。`ProjectItem` 结构不变，`ProjectSelect` 无需改动（仍按 name/code 客户端模糊搜索）。`keyword/skip/limit` 入参移除（wecom 无服务端模糊搜索，下拉由前端过滤）。
  - 依赖：wecom 接口需企业微信 Smartsheet 已配置（corpid/corpsecret）；未配置时返回 `{code:1}`，前端 Toast「获取项目列表失败」。

- **2026-07-28 — 新增「猜你想问」功能（我要摇人对话框）**
  - 需求：进入「我要摇人」（CallView/ChatPanel，scene=call）对话框时，若输入框无内容，随机展示问题池中 5 条；若输入内容命中关键字，则按关键字检索问题池展示匹配项；点击任一条目，该问题立即作为一条用户提问消息渲染进会话框（复用现有 `send` 发送流程，AI 正常作答）。
  - 问题池：新建 `shared/data/suggestedQuestions.ts`，内置 `SUGGESTED_QUESTIONS`（156 条，源自产品提供的 `suggested_questions.txt`），并提供 `pickRandomQuestions(n)`（Fisher–Yates 随机抽取）与 `matchQuestions(keyword, max)`（包含匹配检索）两个纯函数；前端本地检索，无需后端改动，后续维护该数组即可。
  - 组件：新建 `shared/components/SuggestedQuestions.tsx`——主流 AI 引导对话框式悬浮框（标题「猜你想问」+ 灯泡图标 + 可换一批按钮 + 问题列表，条目右侧箭头），`onPick` 回调由父组件接管发送。
  - 集成（`shared/components/ChatPanel.tsx`）：新增 `randomPool` state（进入界面时 `pickRandomQuestions(5)`）；渲染前计算 `suggestedList`——`input` 非空走 `matchQuestions`（最多 8 条），空输入且 `messages.length === 0` 展示 `randomPool`；在 `.chat-input-bar` 内顶部渲染 `<SuggestedQuestions>`，空输入随机场景传入 `onRefresh` 实现「换一批」，`onPick={(q) => send(q)}` 触发即时渲染为用户消息。
  - 样式（`shared/styles/global.css`）：新增 `.suggested-questions*` 一组——绝对定位于输入栏正上方（`bottom: calc(100% + 8px)`）、白色圆角浮层卡片、进入渐显动画、条目 hover/active 高亮（品牌色 #0052d9 + 浅底 #ecf2fe）、单行省略、最大高度滚动。
  - 校验：ESLint 0 error（ChatPanel.tsx / SuggestedQuestions.tsx / suggestedQuestions.ts）。

- **2026-07-28 — 「猜你想问」优化：空输入常驻 + 隐藏麦克风入口**
  - 优化①（`ChatPanel.tsx`）：去掉「仅新建会话（`messages.length === 0`）才显示」的限制——`suggestedList` 空输入分支直接返回 `randomPool`，`showSuggestedRefresh` 仅判 `!trimmedInput`。即**只要输入框无内容就浮现「猜你想问」**（随机 5 条，可换一批），无论是否已有对话；有输入则按关键字检索匹配。
  - 优化②（`ChatPanel.tsx`）：隐藏底部输入栏的麦克风按钮（语音输入入口），用 JSX 注释包裹该 `<button>`，**voiceMode 相关逻辑（`onVoiceBtnDown`/`createRecognition`/`voiceMode` 行等）全部保留**，便于后续恢复入口即可启用。
  - 校验：ESLint 0 error（ChatPanel.tsx 预存的 4 个 warning 与本次无关）。

- **2026-07-28 — 「猜你想问」交互重构：内嵌布局 + 收起为 FAQ 图标 + 检索防抖**（部分已被下条取代）
  - **布局（方案 A 文档流内嵌）**：`SuggestedQuestions` 从 `.chat-input-bar` 内部的绝对定位悬浮层（`bottom: calc(100% + 8px)`，遮挡对话区约 240px）移出到**消息区与输入栏之间的正常文档流**（`global.css` 删 absolute/z-index，改 `margin: 0 8px 8px` 与输入栏对齐、阴影调轻），消息区自动收缩，零遮挡。**（保留）**
  - **检索防抖**：新增 `debouncedKeyword` state——输入停顿 **200ms**（初版 300ms，后按体验调短）后才更新关键词驱动 `matchQuestions`，输入过程中列表保持不动，消除逐击键刷新造成的抖动；**清空输入立即同步**（不走防抖，避免空窗）。`suggestedList`/`showSuggestedRefresh` 均改基于 `debouncedKeyword`。**（保留）**
  - ~~收起状态机 + FAQ 图标入口~~：本条曾实现「收起按钮 + 点选自动收起为输入栏左下角 FAQ 图标」（`suggestedCollapsed`/`suggestedHiding`/`collapseTimerRef`、`.is-hiding` 缩小动画、`.chat-faq-btn`）。经与产品确认后**已整体回退**，见下条。

- **2026-07-28 — 「猜你想问」按产品口径简化：仅首次会话推荐 + 全部场景限 3 条**
  - **产品口径**：不需要收起按钮与 FAQ 入口；仅在**首次新建对话后的首次聊天**（会话无消息）时做随机推荐；问题输入时的检索功能保留；两种场景均**最多展示 3 条**。
  - **回退**（`ChatPanel.tsx` / `SuggestedQuestions.tsx` / `global.css`）：移除收起状态机（`suggestedCollapsed`/`suggestedHiding`/`collapseTimerRef` 及 `collapseSuggested`/`expandSuggested`/自动展开 effect）、header「收起」按钮与 actions 容器（`onCollapse`/`hiding` props）、输入栏 FAQ 图标按钮、`.is-hiding` 退出动画与 `.chat-faq-btn` 样式。
  - **调整**（`ChatPanel.tsx`）：`randomPool` 改 `pickRandomQuestions(3)`，「换一批」同步 3 条；`suggestedList` 逻辑改为——`debouncedKeyword` 非空走 `matchQuestions(debouncedKeyword, 3)`（检索最多 3 条），否则仅 `messages.length === 0`（首次新建会话）展示 `randomPool`，已有对话且空输入时不再浮现。
  - **保留**：文档流内嵌布局（不遮挡对话区）与 200ms 检索防抖均保留。

- **2026-07-28 — 摇人助手（call）空状态：移除 SOS 图标 + 更新欢迎语**
  - 需求：摇人助手（ChatPanel `scene='call'`）进入对话的空状态去掉 SOS 图标（🆘）与 slogan（「一支穿云箭，千军万马来相见！」），提示语改为「你好，{name||username}，请描述你的问题，小U先帮你初步诊断。」（AI 助手名由「AI」改为「小U」）。
  - **改动**（`ChatPanel.tsx`）：空状态渲染对 `isCall` 条件化——`chat-view__empty-emoji` 与 `emptyTitle` 仅非 call（系统任务场景）渲染；call 场景只保留一行 `chat-view__empty-sub` 问候语。系统任务（tasks）场景的 🤖 图标与原有文案保持不变。
  - 校验：`tsc --noEmit` 通过；ESLint 0 error（ChatPanel 预存 4 个 warning 与本次无关）。

- **2026-07-28 — 摇人助手（call）Navbar 标题改为会话标题（新建会话为空，超长省略）**
  - 需求：原来固定显示「我要摇人」的 Navbar 标题，改为——新建会话时显示「新建会话」，已有会话则显示该会话标题；标题文字过长时省略。
  - **store**（`stores/workbench.ts`）：新增 `conversationTitle: string` 字段与 `setConversationTitle(title)` action（初始为「新建会话」）。
  - **写入标题**（`ChatPanel.tsx`）：① 加载会话（`getConversation`）成功后 `setConversationTitle(full.title)`；② 新建会话（`conversationId === null`）标题显示「新建会话」；③ 首条消息建会话（`wasNew`）后同步 `setConversationTitle(userContent.slice(0, 40) || 'AI 对话')`（因 `convRef` 命中后加载 effect 会直接 return，不会自动写标题，故在此主动设置）。
  - **读取标题**（`CallView.tsx`）：Navbar `title` 由固定字符串改为 `<span className="call-navbar-title">{conversationTitle}</span>`（已 import 并解构 `conversationTitle`）。
  - **样式**（`global.css`）：新增 `.call-navbar-title`（`max-width: 60vw` + `text-overflow: ellipsis` + `white-space: nowrap`），实现超长省略。
  - 校验：`tsc --noEmit` 通过；ESLint 0 error（ChatPanel 预存 warning 与本次无关）。

- **2026-07-29 — 根治 AI 会话流式输出/长回复生成的剧烈抖动（标准档：滚动+memo+降级渲染）**
  - **根因**：① 摇人 `ChatPanel.tsx:142` 的 `scrollIntoView({behavior:'smooth'})` 被每个流式 token 频繁触发，smooth 动画排队被打断重启 → 滚动抖动；② 每次 token `setMessages(prev => prev.map(...))` 复制整数组，所有历史气泡随流式重渲染；③ 最关键的共有根因——`MarkdownRenderer`（react-markdown+remark-gfm）对**整段**文本每次全量重解析，文本越长主线程越阻塞 → 掉帧（日报周报页无 smooth 滚动，抖动主因即此）。
  - **方案 A（摇人滚动）**：`scrollToBottom` 改 `behavior:'auto'`（瞬时置底，无动画排队）；新增 `atBottomRef` + 滚动监听，仅当用户「贴底」时才自动跟随（上滑看历史不打扰）。
  - **方案 B（memo 隔离）**：抽出模块级 `MessageBubble`（`React.memo`），`setMessages` 仅最后一条 `content/streaming` 变化、其他消息引用不变 → 历史气泡跳过整列表重渲染；`toggleReaction`/`copyContent` 改 `useCallback` 稳定引用，`editAndResend` 经 `editAndResendRef` 暴露稳定 `handleEditSave`，避免 memo 失效。
  - **方案 C（降级渲染，两页通用最关键）**：`Message` 接口加 `streaming` 标记——流式创建助理消息置 `streaming:true`、流结束 `finally` 置 `false`；**流式中**该气泡用纯文本（`white-space:pre-wrap`）即时渲染，**完成后**转 `MarkdownRenderer` 全量渲染（消除 Markdown 主线程长任务）。日报周报页 `DailySummaryAgent.tsx` 抽出 `ReportStreamCard`（`React.memo`）：`isStreaming` 时用纯文本增量渲染、完成后 `MarkdownRenderer`，避免整页随每个 chunk 重渲染 + Markdown 全量重解析。
  - 效果：摇人消除滚动抖动 + 历史消息零重渲染；日报周报页消除 Markdown 重解析卡顿；均不引入新依赖。校验：`tsc --noEmit` 通过；ESLint 0 error（ChatPanel 预存 `navigate`/`renameConversation` 未用 warning 与本次无关）。
  - 校验：`tsc --noEmit` 通过；ESLint 0 error（ChatPanel 预存 4 个 warning 与本次无关）。

- **2026-07-28 — 修复摇人对话页上传压缩包/文件被当成图片渲染**
  - 现象：在「我要摇人」对话页点「上传文件」选 .zip 等压缩包（含 60MB+ 大文件），前端气泡里把它渲染成 `<img>` 破图，并提示「图片已上传」，看上去「被识别为图片」。
  - 根因：前端 `ChatPanel.handleFileChange` 把「上传文件」按钮（`accept="*/*"`）选中的**任意文件**一律当作 `imageFile` 传给 `send`；`send` 不区分类型，统一 `imageUrl = URL.createObjectURL(file)` 并以 `<img>` 渲染。zip 不是图片，浏览器无法解码 → 破图。文件大小（60MB）与此无关，判定缺失在「类型区分」，不在大小。后端 `/qa/upload`（`ai/api/router.py`）本身正确：存 MinIO 时 `content_type=f.content_type`（zip 为 `application/zip`），且只对 `_is_image_file`（.jpg/.png/.gif/.webp/.bmp）走 VLM 看图描述，zip 不会误送视觉模型，故「识别为图片」纯属前端展示层问题。
  - **改动**（`shared/components/ChatPanel.tsx`）：`send` 新增按 MIME 区分——`isImage = imageFile.type.startsWith('image/')`；图片才挂 `imageUrl` 走 `<img>` 并提示「图片已上传」，非图片改为挂 `attachment: { name, size }` 并提示「文件已上传」；`Message` 类型新增 `attachment` 字段。渲染层新增非图片文件卡片分支（`📎 文件名 + 大小`，KB/MB 自适应）。`global.css` 新增 `.chat-bubble__file*` 样式（与 `.chat-bubble__img` 风格一致）。
  - 校验：ESLint 0 error；`tsc --noEmit` 通过。

- **2026-07-28 — 上传增加 100MB 大小限制 + 进度条百分比**
  - 需求：限制文件上传不超过 100MB（超限提示），大文件上传增加进度条/百分比，方便直观感受进度。
  - 大小限制（`ChatPanel.handleFileChange`）：`MAX_FILE_SIZE = 100 * 1024 * 1024`，选中文件后先校验 `file.size`，超过则 Toast 错误提示「「文件名」(xxMB) 超过 100MB 上限，请压缩或拆分后重试」并清空 input，不再上传。该限制对「上传文件/从相册/拍摄」三个入口统一生效。
  - 进度条：`api/ai.ts` 的 `qaUpload` 由 `fetch` 改为 `XMLHttpRequest`（fetch 无法获取上传进度），新增 `onProgress?: (percent: number) => void` 回调，基于 `xhr.upload.onprogress` 上报 0~100 百分比；返回结构由 `Response` 改为 `{ ok, status, data }`（`UploadResult` 接口）。`ChatPanel.uploadImage` 透传 `onProgress`；`send` 中上传前 `setUploading({ name, percent: 0 })`，过程中通过回调更新百分比，结束（finally）清空。新增 `uploading` state（`{ name, percent } | null`）。
  - 渲染：消息列表结束、输入栏上方插入 `chat-upload-progress` 进度条（文件名 + 动态百分比 + 进度填充条，`transition` 平滑）。`global.css` 新增 `.chat-upload-progress*` 样式（轨道/填充/百分比 tabular-nums）。
  - 校验：ESLint 0 error；`tsc --noEmit` 通过。

- **2026-07-28 — 修复生产端大文件上传被 nginx 413 拦截**
  - 现象：前端已放开 100MB 限制，但生产环境上传 >50MB（如 64MB zip）仍失败，前端报「上传失败: 413」。
  - 根因：上传请求走生产路径 `/p/api/ai/qa/upload`（前端生产 base 为 `/p/app/`，拼出 `/p/api/ai`），对应 `deploy/nginx/conf/nginx.conf` 的 `location /p/api/ai/`。该 location 未单独设 `client_max_body_size`，继承 http 块全局 `client_max_body_size 50M`，导致 >50MB 被 nginx 直接返回 413，请求到不了 AI 后端。`/minio-api/` 虽有 1000M 放行，但那是直连 MinIO S3 的路径，前端上传走的是 `/p/api/ai/`，享受不到。
  - **改动**：
    - 线上真身 `deploy/nginx/conf/nginx.conf`（生产域名 `usp.ep-zl.com`，http 块全局 `client_max_body_size 50M`）：在 `location /p/api/ai/`（生产）与 `location /t/api/ai/`（测试）均新增 `client_max_body_size 110m;`（略高于前端 100MB 上限，留 multipart 开销余量）与 `client_body_timeout 300s;`（避免慢速/大文件接收超时）。`/t/api/ai/media/` 仅用于媒体下发，未改。
    - 仓库参考模板 `deploy/nginx/conf/conf.d/app_gateway.conf`（80 端口、docker 占位 upstream，server 级原 `100m`）：同步改为 `client_max_body_size 110m;`，与该线上配置及前端 100MB 限制保持一致，避免边界 413。
  - 说明：AI 后端（FastAPI/uvicorn）默认无请求体大小限制，故只改 nginx 即可。修改后需 reload nginx 生效（`nginx -s reload`）。
  - 校验：配置为纯 nginx 指令，无 lint/编译校验；需在目标环境 reload 后实测一次 >100MB 上传。

- **2026-07-28 — 修复附件上传静默失败（前端提示成功但文件未落 MinIO）**
  - 现象：生产上传返回 `code:0` 与预签名 URL（如 `http://localhost:9000/helpdesk/{session_id}/{文件名}`），前端提示成功，但 MinIO 中找不到该对象。
  - 根因：`backend/app/utils/minio_client.py` 的 `upload_bytes` 捕获 `S3Error` 后仅 `print` 一行并返回 `False`；而 `ai/api/router.py` 的 `/api/ai/qa/upload` **未检查返回值**，继续生成预签名 URL（预签名 URL 只是签名字符串、不要求对象真实存在）并返回 `code:0`。于是 `put_object` 失败（桶不存在/凭据错/不可达等 `S3Error`）被静默吞掉，前端误报成功。
  - **改动**：
    - `minio_client.upload_bytes`：新增 `raise_on_error: bool = False` 参数；`print` 改为 `logger.error`（带 bucket/object 上下文）；`raise_on_error=True` 时上抛 `S3Error`。默认 `False` 保持其它调用方（`image_processor`、`tasks`、`resource_service` 等）行为不变。
    - `ai/api/router.py`：上传调用改为 `raise_on_error=True`，外层 `try/except Exception` 捕获，失败时 `logger.error` 记录并返回 `{"code":1,"message":"文件「x」上传失败：{真实异常}。请检查 MinIO 是否可达、桶 {MINIO_BUCKET} 是否存在、凭据是否正确。"}`。前端 `uploadImage` 依 `code!==0` 抛错 → Toast 直接显示真实原因。
  - 排查指引（无需改代码即可定位历史失败原因）：查看 AI 服务日志中 `上传字节失败:` 一行，其异常即为真实原因（如 `NoSuchBucket`、`AccessDenied`、`Connection refused`）；预签名 URL 的 `<host>:<bucket>` 即文件实际目标（`localhost:9000` 表示 MinIO 与 AI 服务同机）。
  - 校验：ESLint/Py 调用链无新增 error；部署后上传若仍失败，前端将直接报真实异常。

- **2026-07-28 — 定位 AI 附件上传失败的真正根因：AI 服务读 `ai/.env` 而非 `backend/.env`，缺 MinIO 配置**
  - 现象：生产上传返回 `code:0` + 预签名 URL（`http://localhost:9000/helpdesk/...`），但生产 MinIO 无 `helpdesk` 桶（实际为 `usp-helpdesk`），文件找不到；生产 `backend/.env` 里 `MINIO_BUCKET=usp-helpdesk` 是对的。
  - 根因：`ai/run.py:34` 启动仅 `load_dotenv(ai/.env)`，不加载 `backend/.env`；`ai/.env` 原本**没有任何 `MINIO_*`**，后端 `Settings` 回落到默认值 `MINIO_BUCKET=helpdesk`、`MINIO_ACCESS_KEY/SECRET=""`（空凭据）。于是 AI 上传写到不存在的 `helpdesk` 桶（`NoSuchBucket`）且空凭据（`AccessDenied`），被静默失败 bug 吞掉 → 前端误报成功。URL 里的 `localhost:9000`/`helpdesk` 均为默认值，证实 AI 服务未读到 MinIO 配置。此问题自始存在，本地 64MB 上传亦很可能未真正落盘。
  - **改动**（`ai/.env`）：在「后端联动」段后新增 MinIO 段（`MINIO_ENDPOINT/ACCESS_KEY/SECRET_KEY/BUCKET/SECURE/API_PREFIX`），与 `backend/.env` 对齐。生产端需在**生产 `ai/.env`** 加同样一段（`MINIO_BUCKET=usp-helpdesk`、`ACCESS_KEY=wxing`、`SECRET_KEY=15911Lm!`、`SECURE=false`），加完重启 AI 服务。
  - **改动**（`ai/api/router.py`）：上传前新增 `minio_client.create_bucket(settings.MINIO_BUCKET)` 兜底（幂等，先 `bucket_exists` 再 `make_bucket`；失败仅 `logger.warning` 不阻塞），对齐 `resource_service.py` 既有防御写法，避免 AI 服务（启动时不建桶）因桶缺失导致文件未入桶。
  - 备注：本质修复依赖四处同时就位——① 新代码读 `settings.MINIO_BUCKET`（已提交）② 静默失败改透传（已提交）③ `ai/.env` 补 MinIO 配置（本次）④ 上传前兜底建桶（本次）。文档已同步 `docs/工程文档.md`。
  - 校验：`.env` 为配置变更无 lint；重启 AI 服务后实测一次上传，确认 `usp-helpdesk/{session_id}/{文件名}` 出现且响应 `code:0`、URL bucket 为 `usp-helpdesk`。

- **2026-07-28 — 修复部署后上传 500：会话记忆写入未捕获异常**
  - 现象：部署「静默失败修复」后，上传接口返回 HTTP 500。
  - 根因：`ai/api/router.py` 的 `upload_files` 第 3 段「写入会话记忆」（`get_memory_manager()` + `mgr.add_turn()`，走 Redis）**未包 try/except**；第 2（VLM）、4（agent_state）段均有。此前上传在 MinIO 步即假成功、执行流到不了第 3 段；MinIO 修好后执行流推进至此，生产 Redis 不可达/未配置即抛错 → FastAPI 500。
  - 改动（`ai/api/router.py`）：第 3 段包 `try/except Exception`，失败仅 `logger.error`、不阻塞上传响应（文件已落 MinIO，记忆缺失仅影响后续对话上下文）。
  - 校验：Py 调用链无新增 error；重启 AI 服务后上传应返回 `code:0`。若仍 500，需看 AI 服务日志 traceback 定位其它未守护点（如 `get_presigned_url`）。

- **2026-07-28 — 修复上传后 AI 不回复：`_load_agent_state` 对不完整 agent_state 抛 KeyError**
  - 现象：附件上传成功后，AI 不回复（空气泡、无报错）。
  - 根因：新会话首个动作为上传时，`/qa/upload` 第4段存入的 `agent_state` 仅 `{"attachments":[...]}`、缺 `session_id`；而 `AiDiagnosisPlatform.pipeline._load_agent_state` 用 `s["session_id"]`（非 `.get`）→ 抛 `KeyError`。ask/stream 的 sse 捕获后 yield `event: error`，前端对该事件静默 `continue`、error 的 data 无 token/content → 空气泡。此前被 Section 3 的 500 掩盖（500 时 send 提前 return、未走到 ask）；Section 3 修好后才暴露。
  - 改动：
    - `ai/agents/AiDiagnosisPlatform/pipeline.py`：`_load_agent_state` 增加 `if not s.get("session_id"): return None` 防御，不完整状态视为无状态由 pipeline 重建（attachments 仍被 `_save_agent_state` 的 `existing.get("attachments",[])` 保留）。
    - `frontend/src/shared/components/ChatPanel.tsx`：流式新增 `streamError` 捕获 `event: error` 的 `data.error`；循环结束后 `if (streamError && !acc) throw` → 外层 catch 提示「发送失败」并移除空气泡，不再静默。
  - 校验：ESLint/Py 0 error；重启 AI 服务 + 前端重新构建后，新会话先上传再提问，AI 应正常回复。

- **2026-07-28 — 抽取共享讨论区组件 DiscussionPanel（历史工单 / 系统任务详情页复用）**
  - 需求：历史工单详情页（`pages/call/TicketDetailPage`）与系统任务工单详情页（`pages/tasks/TaskDetailPage`）的讨论区此前各自内联、未复用；要求两端共享同一讨论组件与同一评论数据，当前用户消息靠右、他人靠左。
  - 现状核对：两端评论数据本就走同一后端 `GET/POST /api/tasks/{id}/comments`（历史工单用 `ticket.ticket_id`、系统任务用 `detail.id`，同一工单即同一评论流）；左右布局也已存在（`.detail-chat-row.is-right` + `.is-self`）。缺的是组件复用。
  - **新增** `shared/components/DiscussionPanel.tsx`：内化 `commentText`/`pendingFiles`/file input/`chatMessagesRef` 自动滚到底/`isCurrentUser` 判断（`useAuthStore` 的 username+name）/左右气泡渲染；props：`comments`、`onSend(text, files) => Promise<boolean>`（true=成功→组件清空输入）、`sending`、`disabled`、`placeholder`、`enableAttach`、`enableAI`、`onMessagesClick`、`title`。`@AI` 前缀插入与附件选择在组件内完成，父级只负责 `onSend` 里的 POST/路由。
  - **接入**：
    - `TicketDetailPage`：删内联讨论 JSX + `commentText/pendingFiles/fileInputRef/chatMessagesRef` 及滚动 useEffect；`handleAddComment` → `handleSendComment(text, files): Promise<boolean>`（上传附件 + POST 评论）；渲染 `<DiscussionPanel comments={ticket.comments||[]} onSend={handleSendComment} sending={submittingComment} disabled={!ticket?.ticket_id} enableAttach />`。移除仅讨论区用的 `SafeHtml`/`formatTime` 导入。
    - `TaskDetailPage`：删内联讨论 JSX + `commentText/chatMessagesRef` 及滚动 useEffect + `handleAIClick`（组件内做）；`handleAddComment(text)`/`handleAIDiscuss(text)` 改为接收 text、返回 boolean、不再自清 commentText；`handleSendComment(text, files)` 依 `@AI ` 前缀路由；渲染 `<DiscussionPanel comments={detail.comments||[]} onSend={handleSendComment} sending={submittingComment||askingAI} enableAI onMessagesClick={handleOpenReport} />`。移除仅讨论区用的 `formatTime`/`useRef` 导入（`SafeHtml` 仍用于描述/摘要，保留）。
  - 校验：ESLint 0 error；`tsc --noEmit` 通过。行为保持：左/右布局、附件上传（历史工单）、@AI（系统任务）、诊断报告链接点击（`onMessagesClick`）均不变。

- **2026-07-29 — 摇人对话页上传附件支持追加文字（文件+文字合并上传，改用后端 ack/ai_response）**
  - 现象：「我要摇人」对话页上传文件不支持在对话框追加文字——选中文件即被发送、上传完直接出现在聊天区，无法在发送前补充说明文字。
  - 根因：前端 `ChatPanel.handleFileChange` 选中文件后立即 `send(input, file)`；`send` 先把文件经 `/qa/upload` 上传拿到文件名，再把 `[上传了附件] 文件名` 拼进文字作为用户消息进聊天区，随后又调 `/qa/ask/stream` 重跑一遍诊断。后端 `/qa/upload`（`ai/api/router.py`）本就支持可选 `message` 字段并返回 `ack_message`/`ai_response`、且文件名不注入 LLM 上下文，但前端从未使用 `message`，也未消费 `ack/ai_response`——导致文件名数字污染对话上下文 + 上传与诊断各跑一遍。
  - **改动**：
    - `api/ai.ts`：`qaUpload` 新增 `message` 附带文字参数（非空时后端上传后顺带跑完整诊断并返回 `ai_response`）；新增 `UploadData` 类型（`saved`/`files`/`ack_message`/`ai_response`），`UploadResult.data` 透出 `data?: UploadData`。
    - `ChatPanel.tsx`：新增待发送附件交互——`pendingFile`/`pendingImageUrl` state；`handleFileChange` 改为选中挂起（不再立即发送），`clearPendingFile` 释放图片预览 objectURL；输入区顶部新增附件预览 chip（图片缩略图/📎 图标 + 文件名 + ✕ 移除）。新增 `sendWithFile`：文件+文字一起上传 `/qa/upload`（非流式），用户消息 = 附件卡片 + 纯文字（不再拼文件名标签），AI 回复取 `ai_response.message`（文件+文字）或 `ack_message`（只传文件）；`send` 重构为「有附件走 `sendWithFile`、纯文字走原 `/qa/ask/stream` 流式」，删除 `uploadImage` 与文件名拼接逻辑。发送按钮 `disabled` 由「无文字」放宽为「无文字且无附件」。
    - `global.css`：新增 `.chat-pending-file*` 样式（缩略图/图标/文件名/圆形移除按钮）。
  - 行为对照（对齐后端 `/qa/upload` 协议）：只传图片 → 附件卡片 + VLM 初步诊断（`ack_message`）；只传非图片 → 附件卡片 +「暂不支持解析除图片以外的文件类型」提示（`ack_message`）；文件+文字 → 附件卡片 + 完整诊断回复（`ai_response.message`，若触发提单则 `refreshTasks()` 刷新待派单计数）；文件名不再注入对话上下文。
  - 校验：`tsc --noEmit` 通过、ESLint 0 error。后端 `/qa/upload` 无需改动（此前已实现 message/ack/ai_response）。

- **2026-07-29 — 上传附件体验优化（方案一：乐观渲染气泡 + 进度内嵌 + AI 呼吸点占位）**
  - 现象：上一版「选中挂起 → 点发送」后，聊天区要等上传完成才出现消息，期间只有输入栏上方一条进度条在转、附件 chip 还挂着、聊天区空窗，用户「卡在聊天框里不知何去何从」。
  - 改动（`ChatPanel.tsx` + `global.css`）：
    - `sendWithFile` 重构为乐观渲染：点发送即插入**用户气泡**（附件缩略图/文件卡片 + 文字，`uploading/percent` 内嵌进度遮罩）+ **AI「正在分析」占位气泡**；上传进度经 `onProgress` 实时更新到用户气泡；完成后遮罩消失，`ack_message/ai_response.message` 填入 AI 占位气泡；失败则用户气泡标 `failed`（图片红色遮罩 / 文件卡片红色描边）并移除 AI 占位气泡。
    - `Message` 新增 `uploading/percent/failed` 字段；渲染层图片包一层 `.chat-bubble__media`（相对定位 + 半透明进度遮罩：spinner + 百分比），文件卡片加底部细进度条 + 右侧百分比（失败改红色「上传失败」）；AI 占位由「思考中…」文字改为呼吸点动画。
    - 移除旧的输入栏进度条（`uploading` state + `chat-upload-progress` JSX 及对应 CSS）——进度已内嵌到气泡。
  - 校验：`tsc --noEmit` 通过、ESLint 0 error。


